[rootfs]
	wr-installer = image
[wr-installer "image"]
	default-ktype = standard
	compat = wrlinux-*
	default = wrlinux-image-installer
[wr-installer "distro"]
	default = wrlinux-installer
	compat = wrlinux-installer
